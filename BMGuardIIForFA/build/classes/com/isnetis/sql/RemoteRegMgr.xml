<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN" "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">

<mapper namespace="RemoteReg">

	<update id="RemoteReg.insertReg"  parameterType="remoteReg">
         insert into REMOTEREG_MGR(
			create_date
			,key_name
			,subkey_name
			,value_name
			,data_type
			,reg_value
			,job_desc
			,job_type
			,user_idx
	     )values (
	       DATE_FORMAT(NOW(),'%Y-%m-%d %H:%i:%s')
	     , '${key_name}'
	     , '${subkey_name}'
	     , '${value_name}'
	     , ${data_type}
	     , '${reg_value}'
	     , '${job_desc}'
	     , '${job_type}'
	     , ${user_idx}

	     )
	</update>
	
	<update id="RemoteReg.deleteReg" parameterType="int">
		delete from REMOTEREG_MGR
		where job_idx = ${job_idx}
	</update> 

</mapper>
